#include "main.h"

/**
 * _islower - checks if a character is lowercase
 * @c: checks if it is true
 * @Return: reurn 0 or 1
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
		return (1);


	return (0);
}
