#include "main.h"

/**
 * _strlen _ Output length of string
 * @s: A string
 * Return: length
 */

int _strlen(char *s)
{
	int counter, count;

	int counter =  0;

	for (counter = 0; s[counter] != '\0'; counter++)
	
		count++;

	
	return (count);
	

}
